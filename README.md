# Docker
docker tutorial

## Cài node_modules cho backend

### `cd` vào folder be-master

`npm i `

## Cài node_modules cho frontend

### `cd` vào folder fe-master

`npm i --legacy-peer-deps`

## Tiếp theo 

### `cd` ra ngoài 

`docker-compose build `

`docker-compose up -d`
